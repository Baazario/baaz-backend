generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models are added per sprint.
// This service owns the "payment" PostgreSQL schema.
// First migration: CREATE SCHEMA IF NOT EXISTS "payment";
